apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
metadata:
  name: ceph-config

labels:
  - pairs:
      app: ceph
      managedBy: argocd
      managementCodeGroup: chrismlee42-dev
      managementCodeProject: ceph-config

namespace: ceph

resources:
  - ceph-cephcluster.yaml
  # - ceph-cephblockpool.yaml
  - namespace.yaml
